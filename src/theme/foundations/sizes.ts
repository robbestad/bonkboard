import { BREAKPOINT } from "@/theme/foundations/breakpoints";

const container = {
  xs: `${BREAKPOINT.XS}px`,
  sm: `${BREAKPOINT.SM}px`,
  md: `${BREAKPOINT.MD}px`,
  lg: `${BREAKPOINT.LG}px`,
  xl: `${BREAKPOINT.XL}px`,
};

const sizes = {
  container,
};

export default sizes;
