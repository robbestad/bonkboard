import type { SystemStyleObject } from "@chakra-ui/theme-tools";

const baseStyle: SystemStyleObject = {
  w: "100%",
  mx: "auto",
  maxW: "container.sm",
  px: { base: 4, sm: 16 },
};

export default {
  baseStyle,
};
